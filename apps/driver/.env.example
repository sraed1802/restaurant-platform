# apps/driver/.env.example
# Copy to `.env` or `.env.local` for standalone `npm run dev` in this app (port 5175 by default).

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Same project URL rules as admin/customer: must be *.supabase.co (not a trycloudflare frontend tunnel).
